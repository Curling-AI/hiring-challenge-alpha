import dotenv from 'dotenv';

// Carrega as variáveis de ambiente
dotenv.config();

async function main() {
  try {
    console.log('Iniciando Multi-Source AI Agent...');
    // TODO: Implementar lógica principal do agente
  } catch (error) {
    console.error('Erro ao iniciar o agente:', error);
    process.exit(1);
  }
}

main(); 